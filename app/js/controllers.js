'use strict';

/* Controllers */

var angular = angular || {}; // To shut JSHint
var controllers = angular.module('mustacheApp.controllers', []);

controllers.controller('HomeCtrl', ["$scope", "$http", "$location", function ($scope, $rootScope, $http, $location) {
    
}]);